micronaut:
  application:
    name: micronaut-publisher
  metrics:
    enabled: true
    kafka:
      enabled: false
  kafka:
    embedded:
      enabled: true
    bootstrap:
      servers: localhost:9092
    security:
      protocol: PLAINTEXT