micronaut:
  application:
    name: micronaut-publisher
  metrics:
    enabled: true
    kafka:
      enabled: false
  kafka:
    embedded:
      enabled: true
    bootstrap:
      servers: ${KAFKA_BOOTSTRAP_SERVERS}
    security:
      protocol: PLAINTEXT